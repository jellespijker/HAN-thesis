% You can delete the content between \ProvidesFile{user.sty} and \endinput
% This code is specific for the example report.
% This is the place to load your own packages, classes and  create  your commands.
% If you find yourself, repeatably putting your own custom code in here, because
% you're missing it in the main class, create an Issue in the repo, such that
% it can be added for the benefit of every one.
\ProvidesFile{user.sty}
\RequirePackage{tikz}

\usepackage{pgfplots}

\tikzstyle{block} = [draw, fill=RoyalGrey, rectangle, minimum height=3em, minimum width=6em,rounded corners=.15cm]
\tikzstyle{blockgrey} = [draw, fill=RoyalLightGrey, rectangle, minimum height=3em, minimum width=6em,rounded corners=.15cm]
\tikzstyle{blockdashed} = [draw=RoyalLightGrey,dashed, fill=RoyalGrey, rectangle, minimum height=3em, minimum width=6em,rounded corners=.15cm]
\tikzstyle{blockdashedwhite} = [draw=gray,dashed, rectangle, minimum height=3em, minimum width=6em,rounded corners=.15cm]
\tikzstyle{sum} = [draw, fill=RoyalRed, circle, node distance=2cm]
\tikzstyle{input} = [coordinate]
\tikzstyle{output} = [coordinate]
\tikzstyle{pinstyle} = [pin edge={to-,thin,RoyalBlack}]

\endinput
